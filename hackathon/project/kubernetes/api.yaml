apiVersion: apps/v1
kind: Deployment
metadata:
  name: numbers-api
spec:
  selector:
    matchLabels:
      #TODO
  template:
    metadata:
      labels:
        app: numbers-api
    spec:
      containers:
        - name: api
          image: #TODO
